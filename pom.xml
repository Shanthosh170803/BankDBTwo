the new user login and existing user login work correctly
but the admin cant able to login in and it shows 404 error
in my database i insert:
CREATE TABLE admin (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    password VARCHAR(50)
);
 
INSERT INTO admin (username, password) VALUES ('admin', 'admin123');

if i try to login as admin, it shows
http://localhost:9090/OnlineQuizPortal/adminLogin
HTTP Status 404 – Not Found
Type Status Report

Message The requested resource [/OnlineQuizPortal/adminLogin] is not available

Description The origin server did not find a current representation for the target resource or is not willing to disclose that one exists.

Apache Tomcat/9.0.100

i will give my files regard to make a check related to admin login
===index.jsp===
<!DOCTYPE html>
<html>
<head>
    <title>Online Quiz Portal - Login</title>
</head>
<body>
 
<h1>Online Quiz Portal</h1>
 
<h2>Admin Login</h2>
<form action="adminLogin" method="post">
    <label>Admin Username:</label>
    <input type="text" name="username" required><br>
 
    <label>Password:</label>
    <input type="password" name="password" required><br>
 
    <button type="submit">Login as Admin</button>
</form>
 
<hr>
 
<h2>User Login</h2>
<form action="userLogin" method="post">
    <label>Email:</label>
    <input type="email" name="email" required><br>
 
    <label>Password:</label>
    <input type="password" name="password" required><br>
 
    <button type="submit">Login as User</button>
</form>
 
<br>
 
<a href="user/register.jsp">New User? Register Here</a>
 
</body>
</html>
===AdminLoginServlet.java===
package quizportal.servlet.admin;
 
import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import jakarta.servlet.http.HttpSession;
 
import quizportal.dao.AdminDAO;
import quizportal.model.Admin;
 
import java.io.IOException;
 
@WebServlet("/adminLogin")
public class AdminLoginServlet extends HttpServlet {
 
    @Override
    protected void doPost(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {
 
        String username = request.getParameter("username");
        String password = request.getParameter("password");
 
        AdminDAO dao = new AdminDAO();
        Admin admin = dao.login(username, password);
 
        if (admin != null) {
            HttpSession session = request.getSession();
            session.setAttribute("admin", admin);
 
            response.sendRedirect("admin/quizzes.jsp");  // admin dashboard
        } else {
            response.getWriter().println("Invalid admin login");
        }
    }
}
===quizzes.jsp===
<%@ page import="java.util.List" %>
<%@ page import="quizportal.model.Quiz" %>
<%
    List<Quiz> quizzes = (List<Quiz>) request.getAttribute("quizzes");
%>
 
<!DOCTYPE html>
<html>
<head>
    <title>All Quizzes</title>
</head>
<body>
    <h2>All Quizzes</h2>
    <a href="/OnlineQuizPortal/admin/addQuiz.jsp">Add New Quiz</a><br><br>
 
    <table border="1" cellpadding="10">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
        <%
            if (quizzes != null) {
                for (Quiz q : quizzes) {
        %>
        <tr>
            <td><%= q.getId() %></td>
            <td><%= q.getName() %></td>
            <td><%= q.getDescription() %></td>
            <td>
                <a href="editQuiz?id=<%=q.getId()%>">Edit</a> |
                <a href="admin/addQuestion.jsp?quizId=<%=q.getId()%>">Add Question</a> |
                <a href="deleteQuiz?id=<%=q.getId()%>" onclick="return confirm('Delete this quiz?');">Delete</a>
                
            </td>
        </tr>
        <%
                }
            }
        %>
    </table>
</body>
</html>

paths:
  OnlineQuizPortal/src/main/webapp/index.jsp
  OnlineQuizPortal/src/main/webapp/admin/quizzes.jsp
  OnlineQuizPortal/src/main/java/quizportal.servlet.admin/AdminLoginServlet.java

Also in AdminLoginServlet.java has jakarta package are imports, do we need to change it to javax or no need to change?
