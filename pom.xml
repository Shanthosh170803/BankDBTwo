the quiz table is showing now but the logout is not working properly
http://localhost:9090/OnlineQuizPortal/adminLogout
HTTP Status 404 – Not Found
Type Status Report

Message The requested resource [/OnlineQuizPortal/adminLogout] is not available

Description The origin server did not find a current representation for the target resource or is not willing to disclose that one exists.

Apache Tomcat/9.0.100

===quizzes.jsp===(inside admin folder)
<%@ page import="java.util.List" %>
<%@ page import="quizportal.model.Quiz" %>
<%
    List<Quiz> quizzes = (List<Quiz>) request.getAttribute("quizzes");
%>
 
<!DOCTYPE html>
<html>
<head>
    <title>All Quizzes</title>
</head>
<body>
    <h2>All Quizzes</h2>
    <a href="/OnlineQuizPortal/admin/addQuiz.jsp">Add New Quiz</a>
    <a href="<%=request.getContextPath()%>/adminLogout">Logout</a>
 
    <table border="1" cellpadding="10">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
        <%
            if (quizzes != null) {
                for (Quiz q : quizzes) {
        %>
        <tr>
            <td><%= q.getId() %></td>
            <td><%= q.getName() %></td>
            <td><%= q.getDescription() %></td>
            <td>
                <a href="editQuiz?id=<%=q.getId()%>">Edit</a> |
                <a href="admin/addQuestion.jsp?quizId=<%=q.getId()%>">Add Question</a> |
                <a href="deleteQuiz?id=<%=q.getId()%>" onclick="return confirm('Delete this quiz?');">Delete</a>
                
            </td>
        </tr>
        <%
                }
            }
        %>
    </table>
</body>
</html>

===AdminLogoutServlet.java===
     package quizportal.servlet.admin;
 
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.*;
import java.io.IOException;
 
@WebServlet("/adminLogout")
public class AdminLogoutServlet extends HttpServlet {
 
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {
 
        HttpSession session = request.getSession(false);
        if (session != null) {
            session.invalidate();
        }
 
        response.sendRedirect(request.getContextPath() + "/index.jsp");
    }
}

paths: OnlineQuizPortal/src/main/java/quizportal.servlet.admin/AdminlogoutServlet.java
       OnlineQuizPortal/src/main/webapp/admin/quizzes.jsp
       OnlineQuizPortal/src/main/webapp/index.jsp
